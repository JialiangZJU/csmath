LM algorithm

Usage:
    python hw4.py
in LM function:
    LM(_func,x0,eps = 10**(-7),maxIter = 1000)
        _func is the object function : see obj_func
        x0 is the initial value
    LM returns the result